# 残務のドキュメント反映（v4.1.0 以降の変更）

## ユーザー指示（原文・2026-09-21）
- 「残務を進めて」（引き継ぎ確認の直後。引き継ぎ時に挙げた残務の一覧に対する指示）

## 残務の仕分け（実物で確認した事実に基づく）

| 残務 | 扱い | 理由 |
| --- | --- | --- |
| 仕様・ヘルプ・READMEへの反映（既定フォルダー、上書き拒否の警告） | **今回実施** | バージョンに依存しない現行仕様の記述。`docs/spec*.md`、`src/app/help/help*.md`、`README*.md`、`docs/distribution/readme*.txt` は「上書きしない」までは記載済みだが、保存ダイアログの初期フォルダーと警告ダイアログの記載が無い。 |
| `HISTORY.md` / `HISTORY_jp.md` への記載 | **今回実施（見出しは「Unreleased」）** | 次のバージョン番号はユーザーの判断待ち。番号を私が決めない。リリース時に見出しをバージョンと日付へ置き換える。 |
| `docs/distribution/history*.txt` への記載 | **今回は実施しない** | 配布ZIPに入るファイルで、バージョン未定のまま「Unreleased」を入れると、リリース時の置き換え忘れがそのまま配布物に残る。リリース時にバージョン確定後に書く。 |
| 4.2.0 のバージョン決定・タグ・Release | **実施しない** | ユーザーの判断待ち（メモリ: 明示の指示なしにタグ・Releaseを変更しない）。 |
| Public 化、v4.0.0 のタグ・Release の扱い | **実施しない** | ユーザーの判断待ち。 |
| 実ダイアログを操作しての確認（既定フォルダー、警告の見た目、設定ダイアログ、ドロップ確認、ダークタイトルバー） | **実施しない** | 実 exe を起動して画面操作する確認はユーザー自身が行う方針（メモリ: no-manual-exe-ui-testing）。確認項目の一覧をユーザーへ渡す。 |
| 保存ダイアログ内で既存名を選べなくする案 | **実施しない** | 要望ではない見送り中の案。 |

## 実施内容
1. 仕様（`docs/spec.md` / `docs/spec_jp.md`）: 保存ダイアログは表示中の画像のフォルダーで開くこと、上書きを拒否したときに警告ダイアログを出すことを追記。
2. ヘルプ（`src/app/help/help.md` / `help_jp.md`）: 同上。ヘルプは exe に埋め込まれるため、再ビルドして CTest（コントローラー・QML）で確認する。
3. `README.md` / `README_jp.md`、`docs/distribution/readme.txt` / `readme_jp.txt`: 同上（機能一覧の該当行）。
4. `HISTORY.md` / `HISTORY_jp.md`: 「Unreleased」見出しで、HEIC/HEIF 保存の修正、失敗した保存が空ファイルを残さない、保存ダイアログの初期フォルダー、上書き拒否の警告を記載。
5. 実装（`Main.qml`、コントローラー）と各文書の記述が一致することを、コードで照合する（推測で書かない）。

## 確認項目
- 追記した挙動が、実装（`NativeFileDialog` の初期フォルダー、`overwriteRefused` シグナル、`OverwriteRefusedDialog.qml`）と一致すること。
- 日英の文書が対応していること。
- ビルドとテスト（コントローラー 31 件、QML 28 件）が成功すること。
- `git status` で意図した文書だけが変わっていること。ユーザーの未コミット変更（`.gitignore` の `graphify-out/`）はコミットに含めない。

## 結果（2026-09-21 実施）

### 変更したファイル
`docs/spec.md`、`docs/spec_jp.md`、`src/app/help/help.md`、`src/app/help/help_jp.md`、`README.md`、`README_jp.md`、`docs/distribution/readme.txt`、`docs/distribution/readme_jp.txt`、`HISTORY.md`、`HISTORY_jp.md`（日英が対応）。

### 実装との照合（コードで確認した事実）
- 保存ダイアログの初期フォルダー: `QtAppController::initialSaveFolder()` が、現在の画像のファイルがあるフォルダーを返す（ファイルが無い、またはフォルダーが存在しなければ空 = Windows 任せ）。`NativeFileDialog::pickSaveFile()` が `SetFolder` で毎回そのフォルダーに開く。
- 警告ダイアログ: 保存を拒否したときにコントローラーが `overwriteRefused(path, isOriginal)` を発行し、`Main.qml` が `OverwriteRefusedDialog` を開く。文言は「別の名前または別のフォルダーを指定して、もう一度保存してください」（元画像への保存拒否は「別の名前、別の形式、または別のフォルダー」）。
- 文書の記述は上記に合わせた。

### 確認結果
- ビルド（`scripts/build.py`、Qt 6.10.3 MinGW）: 終了コード 0。
- CTest: コントローラー 31 件、QML 28 件、すべて成功（レポートの更新時刻で今回の実行分と確認）。
- 埋め込みヘルプ: `dist/QuickImageView.exe`（ヘルプ編集後の再ビルド）に、追記したヘルプ文が含まれることを実物で確認。
- `git status`: 意図した文書のみ変更。`.gitignore` の `graphify-out/` はユーザーの変更で、コミットに含めない。

### 実施しなかったこと（理由は仕分けの表のとおり）
- `docs/distribution/history*.txt` への記載（配布ZIPに入るため、バージョン確定後）。
- バージョンの決定、タグ、Release、Public 化、v4.0.0 の扱い（ユーザーの判断待ち）。
- 実ダイアログを操作しての確認（ユーザー自身の確認）。

### 次にやること
- ユーザーが次のバージョンを決めたら: `HISTORY*.md` の「未リリース」見出しをバージョンと日付へ置き換え、`docs/distribution/history*.txt` を追記し、`CMakeLists.txt` のバージョンを更新して、リリースの計画書を作る。
