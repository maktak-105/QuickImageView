# ウィンドウ移動時ハング調査 結果

## 公開停止（ユーザー指示1）
- リポジトリ `maktak-105/QuickImageView` を PUBLIC → PRIVATE に変更（`gh repo edit --visibility private`）。
- 確認: 認証なしのアクセスで、リポジトリ・Release 資産（SHA256SUMS.txt）・API のすべてが HTTP 404。
- Release v4.0.0 とタグ v4.0.0 は削除していない（非公開のリポジトリ内に残っている）。公開中に第三者がダウンロード・フォークした可能性は否定できない。

## 原因と修正
- 調査結果・修正内容は計画書「調査結果」「実施内容」を参照。

## 検証（2026-09-21）
- 修正前ビルドで再現: 本体ウィンドウを x=240 へ動かした時点でハング、`crash.log` が約 131KB 増加。
- 修正後ビルドで同じ移動テストを 2 回実施（x=0〜2700 を 60px 刻み、x=1100〜2000 を 15px 刻み。2 台のモニターの境界を含む）。どちらも最後まで応答。
- `crash.log` の比較: 修正前（01:08〜01:17Z）は Binding loop 2,726 件。修正後は新規 10 件中 0 件。残るのは DPI 切替時の `setGeometry` 警告のみで、増え続けない。
- CTest 2/2 成功。`qiv_qml_tests` は 10 件（回帰テスト 5 件を含む）すべて成功。
- `package.ps1`: ZIP に `QuickImageView.exe` を含み、旧名は含まない。`install.ps1`（一時フォルダー）: 旧 `QuickImageViewQt.exe` を削除し新名で配置。
- 移動テストは `SetWindowPos`（マウス操作なし）でウィンドウを動かすもので、起動した自分のプロセスのみ停止した。

## 最小サイズの緩和（ユーザー質問4・回答: 480x320）
- 変更: `src/ui/Main.qml` の `minimumWidth` 860 → 480、`minimumHeight` 560 → 320。
- 検証: 実デスクトップで 200x150（論理）を要求すると、クライアント領域は 600x400 px = 480x320 論理（DPI 125%）で止まった。応答は維持、`crash.log` の増加は 0 バイト。1180x760 への復帰も正常。
- 未検証: 縮めた状態でのメニューバー・情報帯の見た目（崩れの有無）。スクリーンショットは取っていない。ユーザーの目視確認が必要。
- 併せて確認: Main.qml 内の固定幅は 260（メニュー背景）と 108（言語ボタン）で、480 を超える固定幅はない。

## 未検証
- GitHub Actions（名前変更後の CI / Release）は未実行。
- 別構成のマルチモニター（DPI の組み合わせ違い）での確認はしていない。この PC の 2 台構成のみ。
- 手動のUI操作（ドラッグでの実移動）はユーザー側で確認する。

## 判断待ち
- v4.0.0 の扱い: 既存のタグ・Release（ハングを含む）を削除して同名で作り直すか、v4.0.1 を新設するか。
- リポジトリを再び PUBLIC に戻す時期。
