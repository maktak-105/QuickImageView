# QuickImageView Qt Quick移行 引継ぎ v2.0

作成日：2026-08-24  
ブランチ：`codex/qt-quick-migration`  
コミット・プッシュ：未実施

## 現在の状態

既存Win32版を残したまま、Qt Quick版 `QuickImageViewQt.exe` を追加する作業中。ワークツリーには本作業以外の既存変更も含まれているため、リセット・破棄は禁止。

## 実装済み・追加済み

- Qt Quickの暗色UI、日英切替、Help、About、作者ワッペン。
- WICによる通常画像読込。
- Qt Image FormatsによるWebP読込・保存へ移行。Qt版ターゲットとQtテストからlibwebpの直接リンクを削除。
- `qtimageformats`を導入し、`C:\Qt\6.10.3\mingw_64\plugins\imageformats\qwebp.dll`を配置。
- Qt版の保存ダイアログ：PNG、JPEG、BMP、TIFF、WebP、HEIC/HEIF。
- 右90度・左90度・180度回転、左右反転、上下反転。
- リサイズ数値指定。
- フルカラー、256色、グレースケール変換。
- Undo/Redo履歴への編集操作統合。
- クリップボード画像コピー・貼り付け。
- EXIF表示・クリップボードコピーのQt UI接続。
- 数値指定による切り抜き処理を追加中。

## 検証結果

直近の `build.bat qt` は成功し、以下3件が成功した。

- `smoke_self_test`
- `qiv_controller_tests`
- `qiv_qml_tests`

Qt版のWebPテストはQt Image Formatsの`qwebp.dll`を使って成功した。

## 未完了・要注意

- 最後に追加した切り抜き処理（`cropImage`と`cropDialog`）は、追加後のビルド・CTestをまだ実行していない。
- 切り抜きUIは画像上のドラッグ範囲選択ではなく、現状はX/Y/幅/高さの数値入力。Win32版の選択範囲操作への完全移植ではない。
- Qt版のEXIFは`QImageReader::textKeys()`による取得であり、Win32版WICのMake/Model/DateTimeタグ取得と同等か未確認。
- HEIC/HEIFの保存・読込はWindows WIC codec依存。Qt Image FormatsにはHEIC codecが確認できない。
- 貼り付け画像の仮配置・移動・OK/やり直しは未実装。
- JPEG品質、PNG圧縮の入力UI・保存設定は未実装。
- ファイル関連付け、クラッシュログ、高DPI、アクセシビリティ完全検証、配布物最終検査は未完了。
- 目視スクリーンショット確認は未完了。
- `C:\Users\0120025-Z100\source\project\QuickImageView\6.10.3\` はQt導入コマンドの作業ディレクトリ誤指定で生成された未追跡の一時Qtツリー。リポジトリ成果物ではないため、対象を確認してから削除すること。

## 次の作業

1. `git status`で`6.10.3/`の内容を確認し、Qtインストール本体を壊さないことを確認して一時ツリーを除去。
2. `build.bat qt`を実行し、切り抜き追加後のコンパイル・CTestを確認。
3. Win32版の選択矩形に合わせ、Qt版をドラッグ範囲選択へ変更。
4. WICベースのEXIFタグ取得へ変更し、Make/Model/DateTimeのfixtureテストを追加。
5. 貼り付け仮配置、JPEG品質、PNG圧縮、クラッシュログ、高DPI、アクセシビリティ、配布物検査を実装・検証。
6. `git diff --check`、`build.bat`、`build.bat qt`、CTest、Cppcheck、起動確認、日英UI目視を再実施。

## 保持すべき制約

- 既存Win32版を削除・変更しない。
- `msedgewebview2.exe`をプロセス名だけで終了しない。
- 既存未コミット変更をリセットしない。
- Qt版WebPはQt Image Formatsを使用する。Qt版へlibwebpを再追加しない。
- HEIC/HEIFを「対応済み」と断定せず、WIC codecの存在を実機で確認する。
