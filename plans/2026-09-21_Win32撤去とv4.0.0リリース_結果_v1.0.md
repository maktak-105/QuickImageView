# Win32撤去とv4.0.0リリース 結果

## ローカル検証（2026-09-21）
- `scripts/build_qt.py`（QT_ROOT=`C:\Qt\6.10.3\mingw_64`、MinGW 13.1.0）: ビルド成功。対象は `QuickImageViewQt`・`qiv_controller_tests`・`qiv_qml_tests`。
- CTest: 2/2 成功（qiv_controller_tests、qiv_qml_tests）。Win32 の smoke_self_test は撤去済み。
- 出力: `dist/QuickImageViewQt.exe` と Qt ランタイムが `dist/` 直下に配置された。
- `scripts/package.ps1`: ZIP 生成成功（約 21.7MB）。exe・Qt6Core.dll・platforms・imageformats\qwebp.dll・qml・readme・LICENSE・第三者ライセンス・libwebp 通知を確認。`.gitkeep` と `LICENSE.rtf` は含まれない。
- `scripts/install.ps1`: 一時フォルダーへ `-NoRegisterContextMenu` で実行し、Qt ランタイムの配置と、旧 `QuickImageView.exe` の削除を確認。レジストリは変更していない。
- 手動のEXE/UI操作は行っていない。

## 確認した事実と懸念
- ローカル Qt（MinGW 6.10.3）の `QImageWriter` は PNG・JPEG・BMP・WebP を書き込めるが、TIFF・GIF・HEIC は「Unsupported image format」（Qt の TIFF プラグイン `qtiff` が無い）。アプリは HEIC/HEIF のみ WIC へフォールバックする。CI の Qt（MSVC）で TIFF プラグインが入るかは未確認。文書は「TIFF は Qt の TIFF プラグインが必要」と記載した。
- 第三者ライセンス: Qt（LGPL 3 系）の記載を追加したが、法務確認はしていない。

## 未検証
- GitHub Actions（`ci.yml` / `release.yml`）は未実行。リリース前に main への push で CI を確認する。
- Qt 版のスクリーンショットは未作成（実機撮影が必要。README から Win32 画面を外した）。

## 残課題
- `docs/loop/`（旧ループの状態・handoff）は Win32/tests/python 前提の記述が残る。未対応。
- 実行ファイル名は `QuickImageViewQt.exe` のまま（改名はユーザー判断）。
