# loop削除とフォルダ標準化 結果

## 実施結果（2026-09-21・ローカルのみ、push なし）
- loop を削除: `.agents/`、`docs/loop/`（追跡分 59 ファイル）と、ローカルの `skills/`。
- 標準外フォルダを整理: `qml/` → `src/ui/`、`build-tools/`・`installer/`・`6.10.3/`（重複 SDK、約1.18GB）・`aqtinstall.log` を削除、`history*.md` → `HISTORY*.md`。
- 文書: `docs/project-structure.md` を現行構成へ書き換え、`AGENTS.md` の古い記述を修正。

## 検証
- `scripts/build_qt.py`: ビルド成功。CTest は 2/2 成功（最終）。
- 初回の CTest では `qiv_qml_tests` が失敗した。原因は `tests/qml/tst_quickimageview.qml` の `import "../../qml"`（相対 import）。事前の参照検索が `qml/` の形しか探しておらず見逃した。`"../../src/ui"` に修正し、`qml` を含む全参照を再検索して、残りが `dist/qml`（配置先）・`tests/qml`・テスト名だけであることを確認した。
- 初回のデプロイ工程で `dist\qml\Qt\labs\folderlistmodel` の削除が「別プロセスが使用中」で失敗した。再実行で成功（一時的なロックと見られ、原因プロセスは特定できていない）。
- オフスクリーンで本体を 5 秒起動し、QML 読み込みエラーなく動作継続することを確認（UI 操作なし。起動した自分のプロセスのみ停止）。
- `scripts/package.ps1`: ZIP 生成成功（20.7MB）。exe・Qt6Core.dll・platforms・imageformats・qml・readme・LICENSE・第三者ライセンスを確認。
- 手動のEXE/UI操作は行っていない。

## 未検証
- GitHub Actions は未実行（push していないため）。CMake の `QML_FILES` と `windeployqt --qmldir` を変更しているので、push 後に CI で確認が必要。

## 残置と判断待ち
- `.venv/`（標準の除外対象）、`.claude/`、`graphify-out/`（ユーザーのツール出力・Git管理外）、`build/`、`dist/` は残した。
- `plans/` 内の loop 関連を含む過去の計画書は履歴のため残した。
- テンプレートとの差分（今回は未対応）: `src/engine/`（GUI非依存エンジンの分離。現在は `src/app/image_engine.*`）、`assets/` の `{{AppName}}-icon.png` と README 用スクリーンショット（Qt版の実画面が必要）。
