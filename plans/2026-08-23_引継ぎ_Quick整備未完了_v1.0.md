# QuickImageView 引継ぎ — Quickアプリ整備未完了 v1.0

作成日: 2026-08-23  
作業ディレクトリ: `%USERPROFILE%\source\QuickImageView`

## じゅーごさんの指示（原文・順序厳守）

### 追加の修正

1. EXIF情報の背景を単一のカラー（とりあえず黒）文字は白、また下のメッセージもステータスバーみたいにして黒の背景とする
2. 文字フォントが古臭いのでなんかいいフォント無い？特別なフォントをインストールしたくはないのでWindowsのUIで使っているのにして
3. 範囲選択の時、枠の縦横のサイズ（ピクセル数）を表示（枠の左上、XXXX×YYYY）
4. 範囲選択してからコピーするとその範囲だけコピーする
5. 貼り付けの機能確認、元画像の上に重ねて貼っているか？もしそうなら貼り付ける画像の大きさを拡大せず、元画像のサイズと同じ縮尺で貼る、また張った後、貼り付け先の画像の範囲内で移動できるようにする、貼り付け先の位置で右クリックすると「OK」「やり直し」を選択できて「やり直し」を押すと再度移動できる。「OK」押すと通常操作に戻る。もし画像全体を上書きしてるなら重ねて貼れるようにする。
6. 別形式で保存したらそのファイルを再読み込みする（確認不要）、今は元画像が残っている。

ここまで修正できたらテスト項目追加してテストループを走らせる

### その後の順序指定

それが終わったら、QuickアプリのSkillsを使って、アプリのテーマ、アイコン作成、フォルダ、文書・ヘルプの整理・整備を行う。整ったら日英切替ボタンの追加（Quickアプリ準拠）

特にドキュメントはlibwebpを使っているのでライセンスと同梱するファイルを適切にすること

それが終わったらインストーラ作成、MSI形式

インストールは、右クリックメニューに追加はオプション、関連付けも拡張子ごとにオプション、

それからドキュメントに対応する（開くことができる）形式を全て記載（これはWindowsにインストールされているデコーダ次第？）

### 継続実施に関する指示

- しばらく席を外すので止まらなくて指示を継続して実施
- Codexの画面をセカンドに移動したので実施できなかった分だけ再度テスト回して
- 最後の1個を原因調査して修正＆テスト
- 指示したことを続けて
- もう2度と同じことチェックさせるなよ、
- 全部やって中身を確認してから報告だ、レビューするサブエージェント立てて監視させろ

## 完了の条件

上記の順序を守り、各項目を実装した上で、実UI・文書・配布物・MSIの中身を実際に確認し、独立レビュー担当が合格と判定して初めて報告する。ユーザーに途中の確認や検証をさせない。

## 重要な会話上の制約

- ユーザーは、未完成なのに「完了」と報告されたことに強く不満を持っている。
- 逐一のユーザー確認を求めず、実装者が自力で実機確認すること。
- 独立レビュー担当を立て、完了前に再監査させること。
- 会話・報告は日本語。ユーザーの呼称は「じゅーごさん」。
- `universal-ai-guardrails` と `quick-app-template` を作業前に読み、計画・結果を `plans/` に残すこと。

## 現在の状態

### 既存の機能実装（概ね動作確認済み）

- EXIF/ステータス帯の黒背景・白文字、Segoe UI、選択サイズ表示。
- 選択範囲コピー、貼り付けオーバーレイの移動・OK/やり直し、保存先の再読み込み。
- 選択コピーは`CF_BITMAP`に加えて`CF_DIB`も公開するよう修正済み。`feature_036`は`92×55`で連続PASS。
- Releaseビルドと`ctest --test-dir build --output-on-failure`はPASS済み。

### MSI

- 生成済み: `dist/QuickImageView-1.0.0-x64.msi`（827,392 bytes、2026-08-23 14:33:25）。
- WiX定義: `installer/QuickImageView.wxs`、ビルド: `installer/build-msi.ps1`。
- MSI展開監査では、EXE、日英readme/history、MIT License、`libwebp-COPYING`、`libwebp-PATENTS`の同梱を確認済み。
- WiXの`RegistryKey/@Action`は非推奨警告が出る。将来は`ForceCreateOnInstall`/`ForceDeleteOnUninstall`へ置換する。
- **注意**: `installer/QuickImageView.wxs`は途中で機械置換による破損後に修復した。最後の成功ビルドは修復後の`Win64="yes"`構成で通ったが、再度`installer/build-msi.ps1`を実行してMSIを生成し、必ず再検証すること。

## 独立レビューの不合格結果（必ず解消すること）

レビュー担当（subagent `Curie`）は、現状を**未達・不合格**と判定した。以下は再監査で全て合格にする必要がある。

| 項目 | 未達の根拠 |
| --- | --- |
| ダークテーマ | 情報帯以外は標準Windows見た目。タイトルバー、メニューバー、`EN`ボタンが白い。 |
| 日英切替 | `File / Edit`までは変わるが、Quick準拠のダークな右上言語UIではない。英語後の描画が不安定。 |
| アプリ内ヘルプ | `document/help*.md`はあるが、アプリのメニュー・右クリック・ボタンから開けない。MSIにも入らない。 |
| フォルダ構成 | `assets/`に日英UIスクリーンショットなし。`document/distribution.md`と`document/third_party_licenses_jp.md`なし。Pythonテストが`python/tests/`ではなくルート`tests/`。 |
| README日英 | `README.md`が日本語の機能台帳、`README_jp.md`は短く、内容不一致。Quickテンプレートの英語README/日本語READMEになっていない。 |
| document日英 | `spec.md/spec_jp.md`の内容量・内容が不一致。`third_party_licenses.md`に日本語ペアなし。 |
| dist/documents | readme/history/LICENSEのペアはあるが、libwebpのCOPYING/PATENTSがこのフォルダに無い。 |
| テスト品質 | テーマテストは情報帯だけ、言語テストは`File / Edit`だけを確認しており、全体UI・ヘルプ・外観を担保していない。 |

レビュー時のスクリーンショット:

- `%USERPROFILE%\AppData\Local\Temp\QuickImageView-independent-ui-audit.png`
- `%USERPROFILE%\AppData\Local\Temp\QuickImageView-independent-ui-english-audit.png`

## 作業途中のファイルと注意点

### UI

- `core/native/main.cpp`
  - 現時点の言語ボタンは`BS_PUSHBUTTON`で、レビュー上「白い標準Windowsボタン」。
  - `kCommandToggleLanguage = 1200`、`g_englishUi`、`ToggleLanguage(HWND)`がある。
  - 次の実装案（まだ未適用）: `BS_OWNERDRAW`のダークなピル型言語ボタン、ヘルプボタン、`DwmSetWindowAttribute(DWMWA_USE_IMMERSIVE_DARK_MODE)`、`WM_DRAWITEM`。
  - 直前の大きな`apply_patch`は照合失敗しており、**そのパッチ内容は適用されていない**。
  - 実装後はUIをキャプチャし、タイトルバー・メニュー・右上ボタン・情報帯が一貫してダークであることを確認する。

### 文書・フォルダ

- 途中で追加済み: `assets/QuickImageView-icon.svg`、`document/help.md`、`document/help_jp.md`、`.gitattributes`、HISTORY日英、dist history日英。
- これらは存在だけでは不十分。Quickテンプレートの英語ベース／`_jp`日本語ペアに内容を同期する。
- `README.md`はテスト台帳としても使われている。英語READMEへ変える場合は、機能台帳を別ファイルへ移し、`tests/catalog.py`、`tests/rebuild_catalog.py`、`tests/operations.json`、`tests/ui_test.py`を同時に更新すること。片方だけ変更してテストを壊さない。
- テンプレートは`%USERPROFILE%\.codex\skills\quick-app-template\`にある。少なくとも`01_フォルダ構成.md`、`02_README.md_テンプレート.md`、`03_配布用readme.txt_テンプレート.md`、`04_変更履歴_テンプレート.md`、`05_document_テンプレート.md`、`08_gitignore・LICENSE_テンプレート.md`を読む。

### 生成物・作業ツリー

- 作業ツリーは多数の未コミット変更があり、既存の変更を捨てないこと。
- `dist/msi-stage/`と`dist/QuickImageView-1.0.0-x64.wixpdb`は生成物。削除する場合は対象を明示確認してから行う。
- `docs/loop/handoff.md`などは以前から変更済み。無関係な上書き禁止。

## 推奨する次の実行順

1. レビュー担当を再開または新規に立て、上記不合格表を完了条件として保持させる。
2. `core/native/main.cpp`を実装し、タイトルバー、クライアント、メニュー、右上の言語ボタン、ヘルプ導線を実際にダーク化する。ヘルプはアプリ内ダイアログまたは同梱HTML/テキストを開く実装とし、MSIへ同梱する。
3. `assets/`に日英UIの実スクリーンショットを作成する。SVG原本だけでは不可。
4. `README.md`英語／`README_jp.md`日本語、`document/`日英ペア、`dist/documents/`日英配布文書をテンプレートに合わせて全面同期する。libwebpのCOPYING/PATENTSも`dist/documents/`へ置く。
5. Pythonテストを`python/tests/`へ移すか、現構成を保つ必然性を文書化する。移行するなら全スクリプト参照を更新する。
6. UIテストを強化し、ダークテーマ、言語切替後の全メニュー／情報帯／ヘルプ起動、MSI展開内容を検証する。
7. Releaseビルド、CTest、UIテスト、MSI再生成・管理展開を実行し、レビュー担当に再監査させる。
8. 全項目PASSの根拠（コマンド出力、スクリーンショット、配布ファイル一覧）を`plans/`の新しい結果文書へ記録してから、初めてユーザーに報告する。

## 直近の検証コマンド

```powershell
cmake -S . -B build -G "MinGW Makefiles"
cmake --build build --config Release
ctest --test-dir build --output-on-failure
python .\tests\rebuild_catalog.py --readme .\README.md --output .\tests\operations.json
.\installer\build-msi.ps1 -OutputPath .\dist\QuickImageView-1.0.0-x64.msi
```

MSIの実展開は管理者権限が必要。右クリック・関連付けは既定で未選択であることを確認する。
