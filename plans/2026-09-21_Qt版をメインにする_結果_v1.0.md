# Qt版をメインにする 結果

## 実施結果（2026-09-21・ローカルのみ、push なし）
- ローカル `main` に `origin/qt` をマージし、衝突 20 ファイルを解消した。
- Win32版は `win32-legacy`（897dcfa = 旧 `origin/main`）に退避した。

## コミット状況
- `.git/hooks/pre-commit` の `precommit_scope_guard`（agent-loop のレーン書き込み範囲ガード）が `CODEX_LANE` 未設定でマージコミットを拒否した。
- ユーザー指示（2026-09-21・原文）: 「レーン設定を廃止してコミット」。
- 対応: ローカルの `.git/hooks/pre-commit` を外し（退避バックアップあり。`install_precommit.py` で再導入可能）、マージをコミットする。`docs/loop/`（レーン登録簿・リース等）と `.agents/` のファイルは削除していない。削除範囲はユーザー確認待ち。
- 影響: このリポジトリの全ブランチで、レーンの書き込み範囲ガードが効かなくなる。

## 検証
- `scripts/build_qt.py`（QT_ROOT=`C:\Qt\6.10.3\mingw_64`、MinGW 13.1.0）: ビルド成功。Win32 `QuickImageView`、Qt `QuickImageViewQt`、`qiv_controller_tests`、`qiv_qml_tests` の全ターゲット。
- CTest: 3/3 成功（smoke_self_test、qiv_controller_tests、qiv_qml_tests）。
- `scripts/package.ps1 -Qt`: ZIP 生成成功。exe・Qt6Core.dll・platforms\qwindows.dll・readme.txt・readme_jp.txt・LICENSE.txt を確認。
- 手動のEXE/UI操作は行っていない。

## 未検証
- GitHub Actions（`ci.yml` / `release.yml`）は未実行。ローカルでは Actions の MSVC 構成（`win64_msvc2022_64`）を再現していない。
- `scripts/install.ps1` は未実行（レジストリを変更するため）。

## 未対応（ユーザー判断待ち）
- 計画書「未対応」の項目を参照。
