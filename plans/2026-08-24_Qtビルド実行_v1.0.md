# Qtビルド実行 v1.0

作成日: 2026-08-24  
作業ディレクトリ: `%USERPROFILE%\source\QuickImageView`  
ブランチ: `qt`

## 実施

`python build-tools/build_qt.py`（`build.bat qt` 相当）。SDK は `%USERPROFILE%\tools\Qt`。

初回は `smoke_self_test` が Win32 `QuickImageView.exe` を見つけられず Not Run。`build_qt.py` のビルド対象へ `QuickImageView` を追加して再実行。

## 結果

- `QuickImageViewQt.exe` 生成: `%USERPROFILE%\source\QuickImageView\dist\binary\QuickImageViewQt.exe`
- CTest 3件 PASS: `smoke_self_test`、`qiv_controller_tests`、`qiv_qml_tests`
- `windeployqt` 実施。`Qt6Core.dll`、`platforms/qwindows.dll`、`imageformats/qwebp.dll` を確認
