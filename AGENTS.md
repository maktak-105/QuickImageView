# QuickImageView 作業ガイド

QuickImageViewリポジトリで作業するAIエージェント向けの要約。詳細は計画書・仕様書を参照し、このファイルへ重複して記載しない。

## 基本方針

- 製品バージョンは`v4.0.0`、技術基準年は2026年。
- Qt 6 / Qt Quick / QMLをUIに使用し、画像処理はGUI非依存のC++へ分離する。
- WICを画像処理に使用する。WebPはQtのWebP画像フォーマットプラグインで扱い、外部libwebpに依存させない。
- 現行どおり、GitHub Releasesへ署名なしZIPを配布する。MSIX・有料コード署名は採用しない。
- `main`はQt Quick版。旧Win32版はv3.x系のリリースタグ（v3.1.3まで）に残っている。

## 配置ルール

- `src/app/`：C++エンジン、Qt連携、エントリーポイント、埋め込み用`help/help.md`・`help/help_jp.md`、アイコン
- `qml/`：QML画面・共通部品
- `assets/`：アイコン、SVG、`maktak105-V04-01.jpg`、日英スクリーンショット。配布ZIPへ入れない
- `docs/`：開発者向け日英文書。日本語版は`_jp`接尾辞
- `plans/`：計画書・実施結果。既存計画を上書きしない
- `tests/`：C++テスト・QMLテスト
- `scripts/`：ビルド・Qtデプロイ・配布処理
- `build/`：中間生成物、Git管理外
- `dist/`：完成した実行ファイルとQtランタイム、Git管理外（`dist/.gitkeep`のみ管理）
- `docs/distribution/`：配布用日英文書・ライセンス、Git管理対象

## UIと品質

- Quickシリーズの暗色テーマ、角丸部品、シアン系アクセント、日英切替、中央モーダルAboutを実装する。
- Aboutには`Ver. 4.0.0`、開発環境、制作者、作者ワッペン、OKボタンを表示する。
- QML主要部品には固定`objectName`または`Accessible.name`を付け、状態遷移をテスト可能にする。
- ファイル選択・保存・クリップボード・長時間処理はFake/Mockへ差し替え可能にする。
- CIでC++テスト、QMLテスト、静的解析を実行する。
- クラッシュログ、アクセシビリティ、キーボード操作、高DPIを検証する。

## 作業と検証

### 必ず行うこと

1. 作業開始時に`git status`、ブランチ、対象ファイル、最新計画を確認する。
2. 変更前に、関連する仕様書・計画書・既存実装を確認する。
3. 実装後に、関連文書・バージョン・日英ペア・ビルドスクリプトの整合性を確認する。
4. 変更後に次を実行する。

```powershell
git diff --check
build.bat
build.bat qt
```

5. Qt移行後はCMake/CTest、Qt Quick Test、静的解析、起動確認、日英UI、配布ZIPも検証する。
6. 実行できない検証は理由と代替確認を`plans/*_結果_*.md`へ記録する。
7. 最後に差分と`git status`を確認し、作業結果を日本語で報告する。

### 判断ルール

- ユーザーの依頼範囲を優先し、未指定の機能追加・構成変更・公開操作を行わない。
- 仕様が不明な場合は、コードや参照文書から確認できる範囲を調査し、重要な判断は計画書へ記録する。根拠のない推測で実装しない。
- 既存のユーザー変更、原本画像、配布物、第三者ライセンスを削除・上書きしない。
- 破壊的操作、既存コミットの改変、外部サービスへの公開は、明示的な依頼がない限り行わない。
- ビルド・テスト・フックが失敗した場合は、原因、未実施項目、代替確認を隠さず報告する。
- 実装していない機能を文書へ記載しない。日英で内容が食い違う場合は両方を更新する。

## Git運用

- ユーザーが求めない限り、コミット・プッシュ・リリースは実行しない。
- コミット前に対象ファイル、差分、テスト結果、`git status`を確認する。
- 破壊的操作、既存コミットの改変、無関係な変更を行わない。

## 主要参照

- 計画書：`plans/2026-08-24_qt-quick-migration_v1.0.md`
- アプリ仕様：`docs/spec.md`、`docs/spec_jp.md`
- Quick共通UI：`quick-app-template`スキルの`11_アプリ仕様.md`
- ビルド・配布：`docs/environment.md`、`docs/environment_jp.md`
