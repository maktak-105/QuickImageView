---
marp: true
---

# QuickImageView 開発計画

## 目的

Windows エクスプローラーから画像を素早く確認・簡易編集できる軽量画像ビューアーを作成する。

## 現状

- 実装コード、プロジェクト定義、テストコードは未作成。
- 現在は要件メモのみ存在する。
- 対応OSはWindowsとする。
- 配布方式はインストーラーを基本とし、インストール時に右クリックメニュー登録を選択できるようにする。
- 技術スタック、ライセンスは未確定。

## 技術選定候補

初期実装はC++17 + MinGW-w64 + Win32 + Windows Imaging Component（WIC）を採用した。追加SDKなしで表示と基本変換を検証できるためである。WebView2 + HTML/CSS/JavaScriptは、UI複雑化時の候補として残す。

WebView2へ移行する場合も、画像処理エンジンとUIを分離し、移行後に表示性能を再検証する。

想定構成は `core/native`（処理エンジン・ホスト）、`templates`（HTML）、`static`（CSS/JavaScript）、`document`（仕様・環境文書）、`plans`（計画・実施記録）、`dist`（配布物）とする。

## 開発ループ

各ループで「計画 → 実装 → ビルド → テスト → 実機確認 → レビュー → 計画更新」を行う。

### ループ完了条件

- ビルドが成功する。
- 追加した機能の自動テストが成功する。
- 既存機能の回帰がない。
- Windows上で基本操作を実機確認できる。
- 未解決事項と次ループの対象が記録されている。

## フェーズ0: 方針確定

1. 対応OSと最低動作環境を決める。
2. WindowsデスクトップUI技術と画像処理ライブラリを選定する。
3. 配布形式（単体実行、インストーラー等）を決める。
4. 対応画像形式と、表示のみ対応・変換対応の区分を確定する。
5. 画像処理失敗時の原本保護方針を確定する。
6. 表示対応形式を、実測した起動時間・メモリ使用量・描画速度を基準に決定する。
7. 変換対応形式はJPG、PNG、TIFF、BMP、GIF、WebP、HEICに限定する。
8. 原本を変更しない保存・ファイル名・保存先の仕様を確定する。

## フェーズ1: 最小ビューアー

1. プロジェクト、ビルド、テスト、READMEを作成する。
2. コマンドライン引数またはファイル関連付けから画像を開く。
3. 画像をウィンドウ内に表示する。
4. 拡大・縮小、ウィンドウ内へのフィット表示を実装する。
5. マウスホイールによるズームと右ドラッグによるパンを実装する。
6. 対応外形式、破損ファイル、読み取り権限エラーを安全に表示する。

## フェーズ2: メタデータと操作性

1. EXIF情報を画面に表示する。
2. EXIF情報をワンクリックでクリップボードへコピーする。
3. ファイル名、パス、サイズ、画像寸法、形式を表示する。
4. キーボード操作、最小ウィンドウサイズ、DPIスケーリングを確認する。

## フェーズ3: 簡易編集

1. 画像サイズ変更（パーセント・ピクセル）を実装する。
2. 切り取りを実装する。
3. JPEG等の圧縮率変更を実装する。
4. 色数変更（フルカラー・256色・グレースケール）を実装する。
5. 形式変換（JPG、PNG、TIFF、BMP、GIF、WebP、HEIC）を実装する。表示対応形式を増やしても、変換対象はこの一覧から増やさない。
6. 保存先を指定できるようにする。
7. 原本を絶対に変更・削除・上書きしない。
8. 出力は必ず別ファイルとして保存し、原本と同じパス・同じファイル名への保存を拒否する。
9. ユーザーが原本を置き換えたい場合は、出力後にエクスプローラー等で手動削除・置換する運用とする。

## フェーズ4: エクスプローラー連携

1. インストーラーを基本配布方式とする。
2. インストール時に「右クリックメニューに追加」を選択できるようにする。
3. インストール後の設定画面から右クリックメニュー登録を追加・削除できるようにする。
4. 選択画像を対象にビューアーを起動する。
5. 起動時の引数、複数選択、既定アプリ設定を検証する。
6. アンインストール時に関連付けやシェル拡張の残骸を残さない。

## フェーズ5: GIFアニメーション方針

初期版では、GIFアニメーションを「先頭フレーム表示」または「再生表示」のどちらかに限定する。再生表示を採用する場合は、再生・停止、フレーム遅延、メモリ使用量、長大なGIFの負荷をテストする。編集・変換時のアニメーション保持は別途検証し、未対応なら明示的に警告する。

## テスト計画

- 単体テスト: 拡大率、座標変換、リサイズ、切り取り、色変換、保存先判定。
- ファイルテスト: 主要形式、追加の表示対応形式、破損ファイル、巨大画像、透過画像、アニメーション画像。
- 性能テスト: 起動時間、初回表示時間、ズーム時の応答、メモリ使用量、巨大画像・大量画像での安定性。
- UIテスト: ズーム、パン、EXIFコピー、編集ダイアログ、キャンセル、上書き防止。
- 実機テスト: エクスプローラー連携、DPI、複数モニター、長いパス、権限エラー。
- 回帰テスト: 各ループでビルドと既存テストを必ず再実行する。

## 初回ループの成果物

1. 技術スタックと最低動作環境を確定する。
2. 空のプロジェクトを作成し、ローカルでビルドできる状態にする。
3. テスト実行基盤を用意する。
4. READMEに開発・ビルド・テスト手順を記録する。
5. 次ループの対象を「画像を1枚開いて表示する」に固定する。

## ループ001完了状況

- プロジェクト骨格、CMakeビルド、C++17/Win32/WICによる画像表示を実装済み。
- CTestのスモークテストは成功済み。
- 実画像を指定した起動スモークテストは成功済み。
- 実施記録は `plans/loop-001.md` に保存した。
- 次の対象はズーム、パン、空状態、読み込みエラー処理とする。

## ループ002完了状況

- ズーム、右ドラッグパン、パン範囲制限を実装済み。
- 空状態と読み込み失敗状態を画面内へ表示するようにした。
- CTestと3種類の起動スモークテストに成功した。
- 実施記録は `plans/loop-002.md` に保存した。
- 次の対象はEXIF情報、エラー種別、画像形式・性能計測とする。

## ループ003完了状況

- 基本メタデータ表示を実装済み。
- 読み込みエラーを4分類し、状態表示を分離した。
- CMakeビルドとCTestに成功した。
- 実施記録は `plans/loop-003.md` に保存した。
- 次は指定形式への別ファイル変換と原本同一パス拒否を実装する。

## ループ004完了状況

- 別ファイル変換のコマンドライン基盤を実装済み。
- 原本と同一パス・既存出力への上書き拒否を実装済み。
- WICエンコーダーを使ったPNG→BMP変換を確認済み。
- 実施記録は `plans/loop-004.md` に保存した。
- 次はビューアー内の右クリック操作と出力先選択UIへ進む。

## ループ005完了状況

- ビューアーの右クリックメニューに別形式保存を追加済み。
- 保存先選択ダイアログを追加済み。
- 上書き確認を表示せず、既存ファイルと原本同一パスを拒否する仕様にした。
- 実施記録は `plans/loop-005.md` に保存した。
- 次はインストーラー、エクスプローラー右クリック登録、実機操作確認へ進む。

## ループ006完了状況

- ユーザー単位のインストールスクリプトを追加済み。
- `-RegisterContextMenu` 指定時だけ右クリック登録する方式を追加済み。
- アンインストールスクリプトと配布手順を追加済み。
- 実施記録は `plans/loop-006.md` に保存した。
- 次はクリーン環境でのインストール検証、実機右クリック確認、総合テストへ進む。

## ループ006検証済み

- Windows PowerShellで既定ユーザー領域へインストールできた。
- HKCUの画像右クリック登録を確認した。
- アンインストール後にQuickImageView専用登録が消えることを確認した。

## ループ007完了状況

- EXIFの代表項目表示を実装済み。
- EXIF取得失敗を画像表示失敗と分離した。
- CMakeビルド、CTest、実画像起動確認に成功した。
- 実施記録は `plans/loop-007.md` に保存した。
- 次は総合テスト、性能計測、配布物の最終確認へ進む。

## ループ008完了状況

- MVPのビルド、CTest、配布物生成、実画像起動を確認済み。
- 原本SHA-256が起動前後で一致することを確認済み。
- インストール・右クリック登録・アンインストールを確認済み。
- 実施記録は `plans/loop-008.md` に保存した。
- 高度な簡易編集、GIFアニメーション、複数画像は次期機能として残した。

## ループ009完了状況

- 単体起動時の「ファイルを開く」メニューを追加した。
- `Ctrl+O` とファイル選択ダイアログを追加した。
- 画像切替時の状態リセットを追加した。
- 保存先の新規作成予約により上書き禁止を強化した。
- 実施記録は `plans/loop-009.md` に保存した。

## ループ010完了状況

- 単体起動時のファイル選択問題を解消した。
- エクスプローラー右クリック登録を実行・確認した。
- アプリ内右クリックメニューを直接表示するようにした。
- パン時の点滅対策を追加した。
- 実施記録は `plans/loop-010.md` に保存した。

## QA監査001

過去の完了判定に、実機操作と証跡が不足していた。再発防止の必須ゲートを `plans/qa-audit-001.md` に記録した。以後、Explorer E2E、単体起動E2E、アプリ内右クリックE2E、描画品質確認の証跡なしにMVP完了を宣言しない。

## 現在の完成判定

**完了保留。**

コードの主要部分は実装済みだが、以下の受入証跡が不足しているため、正式なMVP完成とは判定しない。

- Explorerからの右クリック表示・起動E2E
- 単体起動からファイル選択・表示までのE2E
- アプリ内右クリック単押しと右ドラッグの実操作確認
- パン時の点滅がないことの視覚確認
- EXIFコピー機能
- WebP/HEICの実変換可否
- 正式インストーラー要件

## 未決事項

- WebView2 UIへ移行するかどうか。
- Windowsの対応バージョン。
- 表示対応形式の最終一覧と性能上限。
- HEICの読み書きに必要な外部コーデックの扱い。
- GIFアニメーションの初期対応。
- エクスプローラー連携の実装方式。
- ライセンス、配布方式、自動更新の要否。
