# QuickImageView 変更履歴

## v4.2.0 — 2026-09-21

- v4.1.0 で常に失敗していたHEIC/HEIFの保存を修正。Windows のHEIFエンコーダーが求める画素形式へ変換してから書き、失敗メッセージにはWICのエラーコードを併記する。
- 保存に失敗しても、空のファイルや作りかけのファイルを残さないようにし、既存のファイルには一切触れない（残った空ファイルが、次の保存を「既にある」として塞いでいた）。
- 「別形式で保存」のファイル選択を、毎回、表示中の画像があるフォルダーで開くようにした。保存した後は、保存先のファイルがあるフォルダーで開く。画像にファイルが無いときは、Windowsが決めたフォルダーで開く。
- 保存先が既に存在する（または画像自身のファイルである）ために保存を拒否したとき、モーダルの警告ダイアログに、理由・対処・ファイルのパスを表示する。状態テキストのメッセージも残す。
- Explorerの右クリックメニューの「QuickImageViewで開く」を、アプリが開く全ての画像（HEIC・HEIF・WebPを含む）で表示するようにした。PCによっては、HEIC・HEIF・WebPで出なかった。登録は、画像全体への1件から、拡張子ごとに変えた。
- 別のアイコンファイル`QuickImageView.ico`が無くなる（v4.1.0 から同梱していない）と、項目のアイコンが白紙の汎用アイコンになる不具合を修正した。`QuickImageView.exe`に埋め込まれたアイコンを使う。
- 以前の版で登録した項目は、次にQuickImageViewを起動したときに自動で修復する（足りない拡張子を追加し、アイコンを書き直し、以前の「画像全体」への登録を消す）。オフにして、もう一度オンにする必要は無い。`scripts/install.ps1`と`uninstall.ps1`も、同じ拡張子ごとの登録にそろえた。

## v4.1.0 — 2026-09-21

- 表示倍率の異なるモニター間でウィンドウを動かすとハングする問題を修正（v4.0.0 にあった不具合）。
- 既定のウィンドウサイズを720×480（最小480×320）にし、ファイル > 設定でウィンドウサイズをピクセルで指定できるようにした。値は`%LOCALAPPDATA%\maktak-105\QuickImageView\settings.ini`に保存し、次回の起動時に使い、いまのウィンドウにも反映する。
- 保存はPNG、JPEG、BMP、TIFF、WebP、HEIC/HEIFに対応。TIFFはWICで書く（Qtのプラグインは不要）。HEIC/HEIFの保存にはWindowsのHEIFエンコーダーが必要。
- 3.xにあってQt版で欠けていた機能を復活: `--convert`によるコマンドライン変換、ダークなタイトルバー、ドロップで表示中の画像を置き換える前の確認、別形式で保存した後の保存先の再読み込み、拡張子が無い場合の補完、HEIC/HEIFの品質指定。
- 配布物を、アプリが実際に使うものだけにした。QtQuick.Dialogsの代わりにWindows標準のファイルダイアログを使い、Svg・TIFF・GIF・ICOの画像プラグインとQMLのツール用メタデータを外し、MinGWのランタイムを同梱する。リリースはローカルと同じくMinGWでビルドする。
- Qt版の編集メニューに無い項目を案内していたヘルプの記述を訂正。

## v4.0.0 — 2026-09-21（取り下げ）

表示倍率の異なるモニター間でウィンドウを動かすとハングするため、取り下げた。v4.1.0を使うこと。

- Win32実装からQt 6（Qt Quick / QML）実装へ切り替え、メインラインにした。
- 配布は署名なしZIP（`QuickImageView-binary.zip`）と、CIが生成する`SHA256SUMS.txt`。
- UIをQt Quickで作り直し、Explorer右クリック登録の設定ダイアログを追加。
- MSIインストーラーと、Win32用のself-testおよびUI検査スクリプトを廃止。
- WebPは、静的リンクのlibwebpからQtのWebP画像フォーマットプラグインへ変更。
- Win32実装はv3.x系のリリースタグ（v3.1.3まで）から取得できる。

## v3.1.3 — 2026-09-20

- フォルダ構成移行後のビルド・配布パスとリリース検証を修正。
- WiX MSIインストーラーをGitHub Releaseへ添付し、SHA一覧へ追加。

## バージョン命名規則

- 1桁目: 機能追加
- 2桁目: バグ修正
- 3桁目: 文書などその他の変更

## 2.1.0（2026-08-24）

- ヘルプの箇条書きにインデントと余白を付け、版数を明記。
- アプリ内でリンクにならないヘルプ原稿リンク文字列を削除。
- アプリ、バージョン情報、EXEメタデータ、インストーラーを2.1.0へ更新。

## 2.0.0（2026-08-24）

- Quickシリーズ準拠のダークUIへ更新し、「ヘルプ > バージョン情報」を追加。
- 日英ヘルプと公式作者ワッペンを実行ファイルへ埋め込み。
- バージョン情報、配布文書、MSI出力名を同期。

## 1.0.0（2026-08-23）

- 範囲コピー、重ねて貼り付け、別形式保存後の再読み込みを追加。
- ダーク情報帯、Segoe UI、アプリアイコン、日英UI切替を追加。
- 右クリックメニューと拡張子別関連付けを任意選択できるMSI定義を追加。
